// CONTENT NEEDED: real founder/host/practitioner/speaker bios + portraits from the
// brochure. Structure is ready — populate each entry and drop the matching photo into
// /public/people/<slug>.jpg (see ASSETS_NEEDED.md).

export type Person = {
  slug: string
  name: string
  role: string
  bio: string
  category: 'founder' | 'host' | 'practitioner' | 'speaker'
}

export const PEOPLE: Person[] = []
